{
  "name": "curra-project",
  "private": true,
  "version": "0.1.0",
  "description": "Curra payroll platform monorepo",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "5.7.2"
  }
}
