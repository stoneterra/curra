{
  "name": "@curra/curra-api",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "migrate": "tsx src/db/migrate.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.js",
    "lint": "echo 'No lint configured yet for @curra/curra-api'"
  },
  "dependencies": {
    "@curra/country-adapter-ug": "workspace:*",
    "@curra/domain-core": "workspace:*",
    "@curra/ledger": "workspace:*",
    "@curra/money": "workspace:*",
    "fastify": "^5.0.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/pg": "^8.16.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
