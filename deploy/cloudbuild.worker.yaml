substitutions:
  _REGION: us-central1
  _AR_REPO: curra
  _WORKER_JOB: curra-disbursement-worker
  _WORKER_IMAGE: curra-worker
  _IMAGE_TAG: latest

timeout: 1800s

steps:
  - id: build-worker-image
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - deploy/Dockerfile.worker
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_WORKER_IMAGE}:${_IMAGE_TAG}
      - .

  - id: push-worker-image
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_WORKER_IMAGE}:${_IMAGE_TAG}

  - id: deploy-worker-job
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - jobs
      - deploy
      - ${_WORKER_JOB}
      - --project=$PROJECT_ID
      - --region=${_REGION}
      - --image=${_REGION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_WORKER_IMAGE}:${_IMAGE_TAG}
      - --set-cloudsql-instances=$PROJECT_ID:${_REGION}:curra-staging-pg
      - --set-env-vars=CURRA_WORKER_MODE=oneshot,CURRA_REQUIRE_EXTERNALS=true,CURRA_PAYOUT_PROVIDER=eversend,CURRA_DISBURSEMENT_BATCH_SIZE=50
      - --set-secrets=DATABASE_URL=DATABASE_URL:latest,EVERSEND_CLIENT_ID=EVERSEND_CLIENT_ID:latest,EVERSEND_CLIENT_SECRET=EVERSEND_CLIENT_SECRET:latest,EVERSEND_WEBHOOK_SECRET=EVERSEND_WEBHOOK_SECRET:latest,EVERSEND_SOURCE_WALLET=EVERSEND_SOURCE_WALLET:latest

images:
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_WORKER_IMAGE}:${_IMAGE_TAG}
