{
  "name": "@curra/country-adapter-gh",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'No tests yet for @curra/country-adapter-gh'",
    "lint": "echo 'No lint configured yet for @curra/country-adapter-gh'"
  },
  "dependencies": {
    "@curra/domain-core": "workspace:*"
  }
}
