{
  "name": "@curra/country-adapter-ug",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.js",
    "lint": "echo 'No lint configured yet for @curra/country-adapter-ug'"
  },
  "dependencies": {
    "@curra/domain-core": "workspace:*"
  }
}
